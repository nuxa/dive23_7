<%= form_for([question, answer], url: question_answer_contributes_path(question,answer), html: {method: "post"}) do |f| %>
    <p><%= "#{answer.a_points.length-1}人がこの回答が役に立ったと考えています" %></p>
    <%= f.submit("投票する") %>
<% end %>
